# Sentry
# Required for uploading source maps during build
# Get this from your Sentry organization settings
SENTRY_AUTH_TOKEN=

# Sentry DSN
NEXT_PUBLIC_SENTRY_DSN=https://47224fe4099d91b0f769ef28be29fc5f@o216503.ingest.us.sentry.io/4510389398536192

# Google Analytics
# Google Analytics 4 Measurement ID (only loads in production when set)
# Get this from your Google Analytics property settings > Data Streams > Web > Measurement ID
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Upstash Redis (Blog View Tracking)
# Required for blog post view tracking
# Create a free Redis database at https://upstash.com and copy these values
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# RevenueCat
# Public SDK API keys for in-app purchases (use platform-specific keys)
# Get these from RevenueCat Dashboard > Project Settings > API keys > App specific keys
# IMPORTANT: Never use Test Store keys in production builds
REVENUECAT_IOS_API_KEY=appl_
REVENUECAT_ANDROID_API_KEY=goog_
REVENUECAT_TEST_API_KEY=test_

# CI
# Set to true in CI environments (e.g. GitHub Actions, Vercel) to disable some development logs/features
# CI=true
